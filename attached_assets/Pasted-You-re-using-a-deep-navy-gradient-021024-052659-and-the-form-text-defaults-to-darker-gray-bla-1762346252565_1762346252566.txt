You’re using a deep navy gradient (#021024 → #052659), and the form text defaults to darker gray/black (#222 or #000). That makes all form labels and headings disappear into the background.

Let’s fix this holistically while keeping the luxury blue aesthetic consistent across the whole page.

🎨 1. Fix Global Background & Text Contrast

In your main stylesheet (styles.css or App.css), add or update:

/* === GENERAL PAGE BACKGROUND === */
body {
  background: linear-gradient(180deg, #021024 0%, #052659 40%, #5483B3 100%);
  color: #EAF6FF; /* light text for contrast */
  font-family: "Inter", "Poppins", sans-serif;
}

/* === HEADERS / TITLES === */
h1, h2, h3, h4, h5, h6 {
  color: #C1E8FF; /* light blue accent */
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.4);
}

/* === PARAGRAPH TEXT === */
p, label, span {
  color: #D6E4FF;
}

/* === FORM BACKGROUND SECTION === */
.form-section {
  background: linear-gradient(180deg, rgba(2, 16, 36, 0.95) 0%, rgba(5, 38, 89, 0.9) 100%);
  color: #EAF6FF;
  padding: 40px 20px;
  border-radius: 16px;
  box-shadow: 0 4px 24px rgba(2, 16, 36, 0.5);
}


✅ Result:

The upper section now softly transitions from navy to bright blue without washing out text.

All text (form labels, headings, descriptions) uses light, readable tones.

🧩 2. Improve Input Field Visibility

Dark backgrounds require glowing or light-bordered input fields.
Add this:

input, select, textarea {
  background-color: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(193, 232, 255, 0.6);
  color: #EAF6FF;
  border-radius: 6px;
  padding: 10px 14px;
  transition: all 0.3s ease;
}

input::placeholder, textarea::placeholder {
  color: rgba(193, 232, 255, 0.7);
}

input:focus, select:focus, textarea:focus {
  outline: none;
  border-color: #C1E8FF;
  background-color: rgba(193, 232, 255, 0.12);
  box-shadow: 0 0 12px rgba(193, 232, 255, 0.4);
}


✅ This gives inputs a subtle glow effect, matching your palette.

🧭 3. Adjust the Form Container Background (Specific Section)

For the Landlord Request Form, wrap it in a container with:

<section class="form-section">
  <h2>Landlord Request Form</h2>
  <p>Submit your property maintenance request online</p>
  <!-- form fields -->
</section>


And your .form-section CSS (as above) will ensure contrast and readability.

✨ 4. Bonus — Make It Luxurious

Add depth and warmth:

.form-section h2 {
  color: #C1E8FF;
  letter-spacing: 1px;
  font-weight: 600;
  text-transform: capitalize;
}

.form-section p {
  color: #A9C7E8;
}

button {
  background: linear-gradient(90deg, #7DA0CA, #C1E8FF);
  color: #021024;
  border: none;
  border-radius: 8px;
  padding: 10px 24px;
  font-weight: 600;
  box-shadow: 0 4px 12px rgba(193, 232, 255, 0.3);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(193, 232, 255, 0.5);
}


✅ Creates a subtle “architectural glow” — consistent with your 9 Star Renovations identity.

🧠 Quick Recap
Element	Color Fix	Result
Header & upper background	Gradient with light text (#C1E8FF)	Clear contrast
Labels & paragraphs	Soft whites (#D6E4FF)	Readable against navy
Inputs	Transparent + light border	Elegant and visible
Buttons	Blue gradient with glow	Luxurious finish